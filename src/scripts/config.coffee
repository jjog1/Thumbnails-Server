config = {}

# Setup the paths needed by the node.js server
config.paths = {}
config.paths.imageRoot = './_public/images'
config.paths.thumbnailRoot = config.paths.imageRoot + "/thumbnails"

module.exports = config